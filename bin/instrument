#!/usr/bin/env node

'use strict'

const Instrumenter = require('../lib/src/Instrumenter').Instrumenter
const file = require('fs').readFileSync(process.argv[2], 'utf8')

console.log(new Instrumenter().instrument(file))
